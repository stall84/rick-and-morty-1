import React from 'react';
import styled from 'styled-components';

export const ListContainer = styled.div`
    box-shadow: var(--level-1), var(--level-2);
    max-width: 400px;
    max-height: 600px;
`;